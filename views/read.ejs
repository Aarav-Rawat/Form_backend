<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full h-screen p-5 text-white">
        <h1 class="text-2xl tracking-tighter mb-2">All User's</h1>
        <a href="/" class="bg-blue-600 px-3 rounded-md text-lg outline-none border-none">Back</a>
        <div class="users flex gap-5 flex-wrap mt-2">
          <% if(users.length > 0){%>
          <% users.forEach(function(user){  %>
          <div class="user bg-zinc-800 w-44 p-2 rounded-md">
          <div class="w-full h-36 bg-zinc-700 rounded-md overflow-hidden ">
            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
          </div>
           <div class="text-xl tracking-tighter"><%= user.name %></div>
           <div class="text-base text-zinc-500"><%= user.email %></div>
           <div class="flex justify-between mt-4">
            <a class="text-zinc-500" href="/edit/<%= user._id %>">Edit</a>
            <a class="text-red-600" href="/delete/<%= user._id %>">Delete</a>
           </div>
          </div>
          <%})%>
          <%}%>
            
        </div>
        </div>
</body>
</html>